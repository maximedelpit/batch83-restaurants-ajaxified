// TO DO: Remove the restaurant li from the DOM
// We select the li with the right data-restaurant-id attributes and remove it
document.querySelector("[data-restaurant-id='<%= @restaurant.id %>']").remove();
