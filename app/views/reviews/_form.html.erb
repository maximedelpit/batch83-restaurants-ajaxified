<%= simple_form_for [ restaurant, review ], url: "/restaurants/#{restaurant.id}/reviews", method: :post, remote: true do |form| %>
  <%= form.input :content, as: :text %>
  <%= form.button :submit %>
<% end %>
